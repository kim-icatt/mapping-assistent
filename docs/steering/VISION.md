# Mapping Assistant Vision

> Generated by `steer-vision`. Refine this document as the product evolves — do not regenerate from scratch.

Last updated: 2026-06-24

## Product Purpose

Mapping Assistant enables technical administrators at Dutch municipalities to define complete field-by-field mappings between source and target systems, including data transformations. The application guides administrators through inspecting schemas, configuring transformations, and reviewing mappings, and produces exportable output that downstream systems can process directly. Integrated AI suggestions accelerate the mapping process; every suggestion is reviewed and confirmed by the administrator before being applied.

The project has transitioned from PoC to an actual funded product. The current focus is on reproducing a real-world mapping use case (where the mapping has already been established manually) and establishing the repeatability needed to serve additional applications beyond the initial case.

Mapping Assistant is built by ICATT, commissioned by Dimpact.

## Target Users

| Actor | Context | Primary goals |
|---|---|---|
| Technical administrator (Dutch UI term: "functioneel beheerder") | Employed at a Dutch municipality; responsible for maintaining data mappings between internal and external systems | Complete a source-to-target field mapping, export the result, and update it when schemas change |

## Core Principles

1. **AI-assisted, human-confirmed** — The AI assistant proposes mapping suggestions; the administrator reviews and confirms each suggestion before it is applied. No mapping is applied automatically.
2. **Visually clear** — The mapping workspace is designed for immediate comprehension. Administrators see full field coverage, unmapped fields, and transformation rules without navigating elsewhere.
3. **Measurable AI performance** — The accuracy of AI suggestions is tracked systematically so that system quality can be demonstrably improved. This is an internal product quality metric, not an administrator-facing dashboard.
4. **Repeatable** — Mapping configurations can be reused for different combinations of source and target systems. The workflow is consistent regardless of the systems involved.
5. **Exportable output** — Every completed mapping produces a structured, machine-readable export that can be processed by external systems without manual transformation.
6. **Responsible AI** — AI is used transparently and accountably, in line with the Dutch Digitaliseringsstrategie: suggestions are explainable, reviewable, and never applied without human confirmation, and their cost and performance are tracked openly rather than treated as a black box.

## Strategic Goals

- [x] **PoC completed:** at least one source system successfully mapped to a target system with a complete field-by-field mapping demonstrated — *achieved; project now has real budget and is in the transition phase from PoC to product*
- [ ] **Real use case reproducibility:** a technical administrator can reproduce a complete source-to-target mapping for an existing real-world case (where the mapping was previously made manually), obtaining an identical or equivalent result
- [ ] **Repeatability across applications:** the mapping workflow can be applied to additional source-target system combinations beyond the initial case, without rework or special-casing
- [ ] **Exploration-first overview:** the mapping workspace supports exploration of source and target system data — administrators can inspect fields, understand data shapes, and navigate schema structures before and during the mapping process
- [ ] **Context-aware AI component:** AI suggestions are based on rich context management; the assistant selects, scopes, and presents suggestions intelligently, handling large OpenAPI specifications efficiently and making it easy for the administrator to evaluate and act on each suggestion

## Bounded Contexts

| Context | Responsibility | Key domain objects |
|---|---|---|
| Source management | Connecting to source systems and inspecting their field schemas | Source system, Source schema, Source field |
| Target management | Defining the target system and the required field structure | Target system, Target schema, Target field |
| Field mappings | Creating, editing, reviewing, and exporting field-by-field mappings with transformation rules | Field mapping, Transformation rule, Mapping set |
| AI assistance | Generating mapping suggestions, proposing transformation expressions, deriving validation rules from field descriptions, and managing context and API costs for large OpenAPI specifications | AI suggestion, Suggestion score, Acceptance ratio, Transformation suggestion, Validation rule |

## Out of Scope

- **Data transport and pipeline execution** — Mapping Assistant defines how fields are mapped; the application does not move, synchronise, or replicate data between systems.

## Open Questions

- Should mapping templates be shareable between municipalities, or private per municipality?
- Which export formats should the output support (JSON, XML, CSV, XSD)?
- Does the AI component use an external LLM API or an internal model?

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
