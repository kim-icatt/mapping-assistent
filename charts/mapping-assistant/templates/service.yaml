apiVersion: v1
kind: Service
metadata:
  name: {{ include "mapping-assistant.fullname" . }}
  labels:
    {{- include "mapping-assistant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "mapping-assistant.selectorLabels" . | nindent 4 }}
